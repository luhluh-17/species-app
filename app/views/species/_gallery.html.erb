<div id="galleries" class="min-w-full mt-4 grid grid-cols-6 gap-6">
  <% @species.galleries.each do |gallery| %>
    <%= link_to url_for(gallery.image), target: "_blank" do %>
      <%= image_tag gallery.image, class: "w-24 h-24 rounded object-cover"  %>
    <% end %>
  <% end %>
</div>
